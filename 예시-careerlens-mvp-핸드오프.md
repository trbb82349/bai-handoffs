# 핸드오프 문서: CareerLens MVP — 업무카드 추천 프로토타입

## 기본 정보

- 작성자: trbb82349
- 작성일: 2026-09-02
- 담당 파트: 업무카드 수집 · 추천 로직 · 프로토타입 구현 (현재 1인 담당)

## 목표

커리어 방향이 정해지지 않은 사용자에게 "하는 일" 중심의 업무카드를 보여주고, 사용자가 고른 카드를 군집화해 직무·산업 도메인을 설명 가능하게 추천하는 MVP를 만든다.

## 한 일 / 결정한 것

- 원티드 채용공고 18건을 수집해 업무카드 74개 초안 제작 — 이렇게 정한 이유: 잡코리아·사람인·그리팅은 자동 수집(WebFetch)이 403 차단·JS 렌더링 문제로 막혀서, 안정적으로 읽히는 원티드만으로 우선 진행
- 계산 방식이 다른 버전 4개(v1~v4)를 병행 개발하며 이전 버전은 그대로 보존 — 이렇게 정한 이유: v1(자카드 유사도+분야 보너스, 완전 오프라인) → v2(Sentence-BERT 코사인+공동출현 결합) → v3(코사인 유사도만 사용) → v4(v3 로직 + 도메인 카드)로 실험을 이어가되, 앞 버전과 비교할 수 있게 지우지 않음
- "분야"(직무 카테고리) 축을 "산업 도메인 9개" 축으로 전환, 카드도 74개 → 77개로 재작성 — 이렇게 정한 이유: 실제 목표가 "학생이 관심 산업을 찾는 것"인데 기존 "분야"가 직무 카테고리였고, 편견 제거 과정에서 산업 맥락까지 같이 지워졌던 걸 발견함
- v4를 Playwright로 실제 브라우저에서 검증 완료 — 모델 로딩, 카드 렌더링, 추천 결과, "계산 과정 보기", 도메인 설명 탭까지 콘솔 에러 0건 확인 (카드 조합 1건 기준)

## 결과물 위치

- `10-projects/careerlens-mvp/src/careerlens-mvp-4.html` — 현재 버전 (로컬 서버로 열어야 함: `python -m http.server 8000`, `file://`로 직접 열면 모델 로딩 안 됨)
- `10-projects/careerlens-mvp/src/careerlens-mvp.html` ~ `careerlens-mvp-3.html` — 이전 버전 (비교용으로 보존, 건드리지 않음)
- `10-projects/careerlens-mvp/output/업무카드-도메인버전-2026-09-02.csv` — 현재 카드 데이터 77개
- `10-projects/careerlens-mvp/notes/도메인-분류-기준-2026-09-02.md` — 도메인 9개 분류 기준·회사 매핑표
- (원본 코드/데이터는 개인 로컬 워크스페이스에 있으며, 이 문서는 요약본입니다)

## 막힌 것 / 미해결 질문

- `MERGE_THRESHOLD`(0.5)가 새 77개 카드 기준으로 적절한 값인지 카드 조합 1건만 테스트해봄 — 여러 조합으로 더 검증 필요
- 도메인당 공고가 대부분 1~3건뿐이라 데이터가 여전히 희박함 — 공고를 더 모을지, 이 규모로 계속 진행할지 결정 필요
- v1의 자카드 유사도를 v3/v4에 합칠지 보류 중 — 자카드만 이식하면 v1의 "잘 묶이는" 효과가 재현 안 됨(진짜 원천은 도메인 보너스였음), 도메인 보너스까지 가져올지는 미결정

## 다음 할 일

- [ ] v4의 `MERGE_THRESHOLD`(0.5)를 여러 카드 조합으로 재테스트
- [ ] 도메인별 공고 수를 늘릴지 결정 (현재 도메인당 1~3건)
- [ ] (선택, 급하지 않음) 잡코리아·사람인·그리팅 공고를 수동으로 2~4건 보완해 사이트 편중 완화

## 다른 파트와의 연결점

현재는 1인 프로젝트라 팀의 다른 파트와 직접적인 연결점은 없음. 다만 다른 팀원이 이 업무카드 데이터를 재사용하려면, `업무카드-도메인버전-2026-09-02.csv`의 컬럼 구조(`id`, `title`, `domain`, `sources` 등)를 먼저 공유해야 함.
