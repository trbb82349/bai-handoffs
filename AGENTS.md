# AGENTS.md — bai-shared 저장소 안내 (AI 에이전트용)

이 파일은 사람이 아니라 **AI 코딩 에이전트**(Claude Code, Codex, Cursor 등)가 읽는 안내입니다. 팀원이 이 저장소를 열어두고 아래 같은 요청을 하면, 그때마다 이 절차를 따르세요.

## "받아와" / "동기화해줘" / "팀원들 작업 보여줘"라고 하면

새로 clone하는 경우와, 이미 받아본 적 있는 저장소를 다시 동기화하는 경우를 구분합니다. **핵심 원칙: 이미 지난번에 요약해준 문서를 매번 다시 읽고 요약하지 않습니다.**

1. **현재 폴더가 이 저장소의 clone이 아니면** (`.git` 없음): `git clone https://github.com/trbb82349/bai-shared.git`을 실행합니다. 처음이라 전부 새 내용이므로, `handoffs/` 안의 문서를 전부 읽고 요약합니다.
2. **이미 clone되어 있으면**, 새로 추가되거나 바뀐 핸드오프 문서만 골라냅니다.
   a. `git pull` 실행 — pull(merge)을 하면 git이 자동으로 직전 커밋을 `ORIG_HEAD`에 기록해둡니다.
   b. `git diff --name-status ORIG_HEAD HEAD -- handoffs/`를 실행합니다. 이번 pull로 새로 추가(`A`)되거나 바뀐(`M`) 핸드오프 문서 목록만 나옵니다.
   c. 이 목록이 비어 있으면 "새로 올라온 핸드오프 문서가 없어요"라고 그대로 알려주고 끝냅니다. (이미 본 문서를 다시 요약하지 않습니다.)
   d. 목록에 있는 파일**만** 열어서 요약합니다.
3. 각 문서를 한국어로 짧게 요약해서 보여줍니다 — 문서의 7개 항목(기본 정보·목표·한 일/결정한 것·결과물 위치·막힌 것·다음 할 일·다른 파트와의 연결점) 중 **"막힌 것"과 "다음 할 일"을 가장 눈에 띄게** 보여주세요. 다음 사람이 바로 이어받을 수 있어야 합니다.
4. 사용자가 특정 프로젝트를 더 보고 싶다고 하면 (요약에 없던 오래된 문서 포함) `projects/<이름>/README.md`와 관련 파일을 열어서 설명하고, 실행 방법(로컬 서버 필요 여부 등)을 그 프로젝트의 README에서 확인해 안내합니다.

## "내 프로젝트 공유해줘" / "핸드오프 문서 써줘"라고 하면

1. 사용자에게 어떤 프로젝트 폴더를 올릴지 확인합니다.
2. 그 프로젝트 폴더를 이 저장소의 `projects/<프로젝트이름>/`에 복사합니다 (원본 폴더는 건드리지 않고 그대로 둠).
3. `handoffs/template.md`를 복사해서 `handoffs/YYYY-MM-DD-이름-핸드오프.md`로 만들고, 사용자와 대화하며 7개 항목을 채웁니다.
   - 원본 대화 전체를 붙여넣지 않습니다. 반드시 항목별로 걸러서 요약합니다.
   - "결과물 위치"에는 로컬 경로가 아니라 이 저장소 안의 경로(`projects/프로젝트이름/...`)를 적습니다.
4. `git add`, `git commit`, `git push`로 올리기 전에 어떤 파일이 올라가는지 사용자에게 보여주고 확인받습니다 (특히 `.env`, API 키, 큰 바이너리 파일이 섞여 있지 않은지).

## 실행 관련 주의사항

- 프로젝트마다 실행 방법이 다를 수 있습니다 (예: `careerlens-mvp`의 v2~v4 HTML은 `python -m http.server`로 열어야 하고 `file://`로 직접 열면 안 됨). 안내 전에 항상 그 프로젝트의 `README.md`를 먼저 확인하세요.
- 이 저장소는 **public**입니다. 비밀번호, API 키, 개인정보는 절대 올리지 않습니다.
- 잘 모르겠거나 애매한 요청은 추측해서 진행하지 말고 사용자에게 짧게 확인합니다.
